"""\
Pyconstruct provides a number of predefined domains. Predefined domains can
simply be loaded by::

    domain = Domain(name)

For instance::

    ocr_domain = Domain('ocr')

Loads the OCR domain, compatible with the OCR dataset.

Currently available:
  - `ocr` : Optical Character Recognition
  - `conll00` : CoNLL text chunking
"""
